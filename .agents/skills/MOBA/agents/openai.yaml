interface:
  display_name: "MOBA"
  short_description: "Repo-specific patterns and workflows for MOBA"
  default_prompt: "Use the MOBA repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true